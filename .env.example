# 复制本文件为 .env，再填写真实 ModelScope Token。不要把 .env 提交到 Git。
LLM_API_KEY=
LLM_BASE_URL=https://api-inference.modelscope.cn/v1
LLM_MODEL=deepseek-ai/DeepSeek-V4-Pro

# Query 改写只需短、稳定的 JSON 输出，因此使用低温度与较小 token 上限。
LLM_TIMEOUT_SECONDS=30
LLM_TEMPERATURE=0
LLM_MAX_TOKENS=500
